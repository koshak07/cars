import React from 'react';
import MediaCard from '../components/MediaCard';

const MainPage = () => {
    return (
        <div className='cars'>
            <MediaCard/>
        </div>
    );
};

export default MainPage;